#include "packet_probe/ipc/ipc_protocol.hpp"

namespace packet_probe {

static_assert(kPacketProbeLogSchema[0] != '\0');
static_assert(kPacketProbeEventSchema[0] != '\0');

}  // namespace packet_probe
